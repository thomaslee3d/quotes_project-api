<% quotes-project = 'rails-api-template' %>

default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>

development:
  <<: *default
  database: <%= quotes-project %>_development

test:
  <<: *default
  database: <%= quotes-project %>_test
